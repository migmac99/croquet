{
  "name": "workers",
  "description": "",
  "private": true,
  "scripts": {
    "prettier": "prettier --write .",
    "dev": "bun run prettier && ./deploy.sh dev",
    "dev:sync": "bun run prettier && ./deploy.sh dev:sync",
    "deploy": "bun run prettier && ./deploy.sh all",
    "deploy:staging": "bun run prettier && ./deploy.sh staging",
    "deploy:synq": "./deploy.sh sync",
    "deploy:mgr": "./deploy.sh mgr",
    "tail": "./deploy.sh tail",
    "tail:synq": "./deploy.sh tail:sync",
    "tail:mgr": "./deploy.sh tail:mgr",
    "tail:staging": "./deploy.sh staging:tail",
    "install": "cd manager && bun i && cd ../synchronizer && bun i"
  },
  "devDependencies": {
    "wrangler": "4.51.0"
  }
}
